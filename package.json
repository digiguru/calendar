{
  "name": "resource-calendar",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "14.2.x"
  },
  "dependencies": {
    "local-storage": "^2.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^5.2.0",
    "react-scripts": "^4.0.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "test:ci": "react-scripts test --ci --reporters jest-junit --reporters default",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "jest-junit": {
    "outputDirectory": "./results/junit/",
    "outputName": "results.xml"
  },
  "devDependencies": {
    "jest-junit": "^12.0.0"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
